services:
  vpn-bot:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - bot-data:/app/data
    environment:
      PORT: "8080"
      DB_PATH: /app/data/orders.db

volumes:
  bot-data:
